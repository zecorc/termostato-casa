; ESP32 room sensor node (WROOM devkit, same board as hub for now)
[platformio]
default_envs = esp32dev

[env]
platform = espressif32
framework = arduino
monitor_speed = 115200
build_flags =
  -I ../common/include

[env:esp32dev]
board = esp32dev
